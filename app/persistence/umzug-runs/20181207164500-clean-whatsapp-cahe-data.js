/* eslint-disable no-unused-vars */

// Same migration like the previous (20181203145000-clean-whatsapp-cahe-data.js)
// one because our code fix haven't work

export default {
  up(query, DataTypes) {
    return null;
  },

  down(query, DataTypes) {
    return null;
  }
};
